// check that the class has a superclass when calling a method

class Base {
  foo() {
    super.doesNotExist(1); // expect: Error at 'super': Can't use 'super' in a class with no superclass.
  }
}

Base().foo();
