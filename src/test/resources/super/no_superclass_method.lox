// check that the superclass method exists

class Base {}

class Derived < Base {
  foo() {
    super.doesNotExist(1); // expect: Undefined property 'doesNotExist'.
    // expect: [line
  }
}

Derived().foo();
